#include <__clang_cuda_builtin_vars.h>
#include <cuda_runtime.h>
#include <iostream>
__global__ void vectorAdd(float*a,float*b,int N){
    int kidx = threadIdx.x+blockDim.x*blockIdx.x;
    for(int )
}
int main(){

}